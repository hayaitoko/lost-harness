# P19

## Status
☑ done

## Branch
fix/p19-msrv

## Findings
M-17 (MEDIUM) — Minimum Rust Version

## Updated
2026-07-28T20:31:03Z

## Files changed
- `src-tauri/Cargo.toml` — `rust-version` bumped from `"1.77"` to `"1.82"`
- `README.md` — toolchain requirement line updated from `Rust 1.77 or newer` to `Rust 1.82 or newer`

## Fix notes
The codebase uses `Option::is_none_or` (stabilized in Rust 1.82) at `dispatch.rs:1273`
while the declared MSRV was 1.77. Under the DECISION = raise MSRV to 1.82, the fix is
purely a metadata update:

- `dispatch.rs` uses `is_none_or` correctly and needs no change under the raised MSRV.
- Only the declared MSRV (`Cargo.toml`) and the README documentation were out of sync.

## Validation
- `cargo build --lib` passes (exit code 0, `Finished dev profile`).
- The single use of `is_none_or` at `dispatch.rs:1273` now agrees with the declared MSRV.
- Only pre-existing warnings remain (88 warnings, none introduced by this change).
- `cargo fmt` skipped — changed files (Cargo.toml, README.md) are not Rust source files;
  no Rust files were modified.

## Follow-ups
None.

## Needs human decision
None — decision was provided in the packet (raise to 1.82).